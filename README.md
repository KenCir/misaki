# misaki

博瀬の架空の彼女「美咲」

ChatGPTのAPIを使って、実際の彼女のように会話してくれます

## 起動方法

1. コンテナをビルドします

```
docker compose build
```

2. スラッシュコマンドを同期します(ほぼ使ってないのでやらなくてもOK)

```
docker compose run --rm app pnpm run deploy
```

3. 起動します

```
docker compose up -d
```
